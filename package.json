{
  "name": "stryker-tz-bug",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "stryker": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.0",
    "@stryker-mutator/vitest-runner": "^9.6.0",
    "vitest": "^4.1.4"
  }
}
